AWS - S3

CRR vs SRR Observation:

Bucket A (ap-south-1)
        │
        │  CRR (will replicate obj to bucket B)
        ▼
Bucket B (eu-west-1)
        │
        │  SRR (new obj only will replicated in bucket C which are created in bucket B, obj from bucket A will not be replicated)
        ▼
Bucket C (eu-west-1)
